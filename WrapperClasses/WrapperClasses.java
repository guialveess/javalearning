package WrapperClasses;

public class WrapperClasses {
    // Eficiencia = Tipo Primitivo
    // Passar um Nulo = Wrapper Classes (Arrays, Bibliotecas, etc)
    // Tipo primitivo oculpa menas memoria / já o wrapper class eu teria que designar o espaço pro objeto
    // Não posso passar valor null para tipo primitivo ( Já no Integer eu consigo )

    public static void main(String[] args) {
        int tipoInt = 1; // tipo primitivo
        float tipoFloat = 1;
        boolean tipoBoolean = false;

        // int - char
        Float tipoFloat2 = 1F; // wrapper classes
        Boolean tipBoolean2 = false;
        Integer tipoInteger = 1;


    }
}